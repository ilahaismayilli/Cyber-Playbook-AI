# Cyber Playbook AI v1.1

This repository contains CyberPlaybook AI — a small Flask app for generating and displaying cyber playbooks.

Contents:
- `outputs/` — application source, templates, and deployment files

Instructions:
1. Create a GitHub repository (public) and paste the remote URL here.
2. Run `git remote add origin <URL>` and push: `git push -u origin main`.

License: add a license file if needed.
